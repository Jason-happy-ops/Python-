Day 1 2025.11.10    外星人飞船
编写事件循环，检测响应特殊的事件
将settings实例分离开来————使代码更容易维护，灵活

Day 2 2025.11.11    外星人飞船
今天编写了ship类，对于初始化里面的ai_game不太理解，之前练的都是把单个变量传给实例，这样的我没见过啊，问了丁h哥，估计明天才能回了
刚才又运行了一下程序，漏洞百出啊！
Ps：今天git终端快把我累死了，老是传不上我的repo，一看原来终端本地是main，却想传到master分支上面————分支管理混乱，用了git merge master命令

Day 3 2025.11.12    外星人飞船
今天利用水课重新回顾了一下类的概念，花了2个小时终于摸到一些皮毛。以下是我对self的理解：__init__方法中的self就相当于一个空的占位符，
等待一个未来的实例去取代他。而昨天那个ai_game我也添加了注释：self.ship=Ship（self）其实就是把主程序的init中的self当作实参传入ship类，
ai_game即被替换为self（主程序的）实例。进而因为只需要主程序中的screen属性，所以没有必要写self.ai_game=ai._game。
Ps：git bash又闹罢工了， ! [rejected]        master -> master (fetch first)
error: failed to push some refs to 'https://github.com/Jason-happy-ops/Python-'
hint: Updates were rejected because the remote contains work that you do not
hint: have locally. This is usually caused by another repository pushing to
hint: the same ref. If you want to integrate the remote changes, use
我仔细思考了一下，这是因为我在Github上修改了readme文件而这个修改没有保存在我的本地，两边更新不同。所以solution就是先git pull origin master
拉取远程仓库的更新，再git push origin master

Day 4 2025.11.14    外星人飞船
今天做了向左向右的飞船操控，这个外星人飞船的项目要告一段落了，里面pygame库的功能都不太了解，学起来很吃力。但在四天总共快20h（加上水课啃概念）
的积累下也是对类的使用有了一点基本的了解，也是为我的半途而废找个借口吧。接下来会去学习数据分析库了，加油！

Day 4 2025.11.14    Collatz函数
今天又写了一个有趣的数学函数，并且加入了try——except验证机制（ValuError）。同时今天也犯了一个错误：
如果函数既定义了形参number，又在内部用input函数给number重新赋值，那么调用函数时传递的实参会被input（）获取
的输入覆盖。以后要注意！

Day 5 2025.11.16    抛掷概率计算
写了一个计算投掷100次骰子出现连续六次正面或反面的概率。一开始脑子空白一片，没啥思路，特别是连续六次如何表示实在不会。问了豆包应该用for循环，循环遍历
加入列表中的每一个元素并进行判断。但是也要表扬一下自己在几个月的练习下已经能熟练运用循环，以及f语法糖。希望以后能多多学习一些代码优化

Day 6 2025.11.17
初探数据分析的matplotlib，之前其实已经学过一点了，现在就是算复习吧。准备向嵌入式发展了，希望数据分析能帮助我在工业数据分析中获得有用的结果。
Ps：还有三天就数模比赛了加油！

Day 7 2025.11.23
数模打完终于有时间坐下来练一练自己的代码能力。说一说自己对于AI code agent的一些想法吧。第一个就是在做的时候直接把cursor免费额度用完了，确实好用。
第二个就是其实除了画图，其他算法我都会去让AI帮我去做，因为计算类我很放心AI的答案，但是在数据搜索层面，（我用的豆包）我并不信任AI，他们的数据可能是捏造的。
所以在让他们写代码层面我会强调使用我的真实数据，并且把搜索到的数据丢给他们让他们学习。刚接触agent的时候觉得有了AI还要个屁的程序员啊，现在发现基础的程序员
确实不被需要了，算法岗反而会更吃香的。我在这次比赛中发现卡脖子的不是代码，而是算法。没有合适的算法就解不出来题目，你觉得AI会帮你找到一个合适的算法吗？不会的
这些你得自己去找，找到之后让AI帮你试错，这一点我相信很多人步骤都做反了。

Day 7 2025.11.23
今天继续练5000个点的随机游走，已经有那么点意思了。现在进步的一点是能基本熟练看懂报错并修改。这点能力我相信是很重要的

Day 8 2025.11.24
今天练习了解析csv文件头，利用r"绝对路径"防止转义可能导致的错误
同时学习了Plotly，理解了其在web交互方面的可视化水平，这个正好我觉得可以辅助昨天说的去写一个AI agent用于爬取搜索权威数据，增强可信度和可读性

Day 9 2025.11.26
练习插队排序。说一下自己在写的时候犯的几个错误。第一个：list out of range 元组的索引超出了范围，第二个：元素后移后没有插入扫描元素的位置。
再理一下插队排序的思路：1.大的往后放，小的往前放。2.左边是已排序的，所以在从右边的乱序中抽取元素时，应当先和最左侧元素比较，以此类推。
3.元素后移会空出位置，留给被扫描的元素

Day 10 2025.11.27
是对Day8的进一步学习，首先理解了reader的作用，他是把每一行的数据转成了一个容器可以用来读取。调用csv.reader把各行列表传输给他。利用enumerate查询
索引和值之后用for循环存储目标列的值，注意！如果是数值用int转换。同时学习了datetime模块，用fig.autofmt_xdate绘制倾斜标签，防止彼此重叠

Day 11 2025.11.29
进一步改进Day10，强化了csv模块的可读性，加了多行注释。本来想引入pandas库再做一个，但是发现自己老是混用csv和pandas块。所以直接用csv模块实现一个软编码
很简单的解决方案：就是把csv.reader换成csv.DictReader。这样的话相当于把表头转成一个字典方便查询文件头，原来的列表索引会出错。

Day 11 2025.11.29
地震散点图，相对路径又弄错了，终端是在Python文件夹运行，但是data在practise文件夹中所以要加一个相对路径practise/
以及复用变量

Day 12 2025.11.30
地震散点图引入plotly进行可视化。我现在对matplotlib和plotly有点混了，以后记忆些核心代码，牢记plotly是交互性的库。学习了嵌套字典如何提取值

Day 13 2025.12.1
哈哈哈哈十二月第一段代码结束了地震散点图。今天引入pandas库，不用再麻烦从列表中取出数据给绘图函数了，现在可以直接在封装数据的DataFrame中读取列名
也算属于一种优化吧。color='震级'表示用渐变色表示震级的不同大小。

Day 14 2025.12.3
第一次API的学习，应用程序接口。真的很神奇啊，只要几行代码你就会得到想要的数据。https://api.github.com/search/repositories?q=language:python+sort:stars+stars:>10000
用这个网址搜索star大于10000的项目。自己去了解了json数据的优点，简洁易懂以字典的形式去展示。用application/+json将其转为json格式

Day 15 2025.12.4
我决定好好理解一下这个可变&不可变类型：不可变数据类型： 当该数据类型的对应变量的值发生了改变，那么它对应的内存地址也会发生改变，对于这种数据类型，就称不可变数据类型。
                                    可变数据类型 ：当该数据类型的对应变量的值发生了改变，那么它对应的内存地址不发生改变，对于这种数据类型，就称可变数据类型。
之前一直不理解为什么有了列表还要有元组，现在从内存优化以及安全方面好像确实很有道理。

Day 16 2025.12.6
马上又要去忙美赛的选拔了，今天上传了一个Q1的文件，除了函数部分，都是自己一个一个代码敲出来的。在可视化部分发现原来excel数据类型为13W+1这种实在不适合做横坐标
那我就去用字符串相关把他做成日期，就是转成多少Days。刚才检查了一下AI写的转化函数，它的注释都写错了，明明upper是转大写，它注释写的是转小写。但我觉得他想的确实比我周到
在几次函数报错后，它提示我应该去调整大小写匹配格式以及可能会出现13W后面为空的情况导致报错。以后可能几天都不会更新了

Day 17 2025.12.8
暂且更一下。写了一个简单的线性回归，用的是2025国赛C题清理后的数据。现在发现程序做算法确实快啊，夸夸夸几个函数就能算出来了。利用sklearn的LinearRegression
就可以算多个因变量的作用结果，很方便。以后可以用正则化优化系数，防止多重共线性的影响（Lasso回归）后面会抽空去做的！

Day 18 2025.12.10
用鸢尾花简单写了一个最最最简单的决策树，但是底层原理我还没有吃透，这两天要学习一下底层原理。其实目前看下来就是一个特征分类

Day 19 2025.12.13
国家公祭日，默哀一分钟。利用matplotlib画了一个鸢尾花决策树的简单可视化视图

Day 20 2025.12.14
继续写正则化表达式。以下是简化步骤：1.import re导入模块    2.re.compile()函数创建一个Regex对象（使用原始字符串）
3.向Regex对象的search()方法传入想查找的字符串，它返回一个Match对象。    4.调用Match对象的group()方法，返回实际匹配文本的字符串

特殊符号记得\转义，出现missing or unbalanced parenthesis 的错误信息可能忘记为分组添加括号

如果多个被搜索的字符串同时出现，那么第一个将会被作为Match返回——————管道字符|,可以用来指定后缀

Day 21 2025.12.17-12.18
根据之前建模C题的已清洗数据依照之前的决策树写了一个实际应用。谈一谈自己学到的东西，第一个就是X输入必须是二维数组！！！那么在用pandas导入的时候就要当心了！
而与之相对的，必须用一维数组去表示y。同时理解了随机种子的含义。那么最后结果做出来的决定系数R2为负数，那么我就想可能用随机森林去做会不会更好更合适一些。

Day 22 2025.12.23
隔了快一周更新。第一个完成的就是随机森林，其实我发现还是很简单的，无非就是换一个函数名，所以python真的很方便！随机森林在回归，分类两边都很有用，
所以在运用之前那需要先行判断！很重要！适合有噪声的数据，第二个完成的是递归函数，虽然只有四行但是我觉得学到了很多东西，首先就是递归必须要有终止条件，其次就是
我在其中理解到了调用栈一点皮毛。说说我的理解，调用栈其实只要在调用函数的时候就会出现，先调用什么函数就先把什么函数压入调用栈，那么如果是在函数体内部调用一个新函数，
就需要暂时停止这一个函数的执行，去运行调用的函数，直到所有函数都被调用完。结束之后依次弹出，从后向前执行。

Day 23 2025.12.25
Merry Christmas!这两天在学习多层次分析法，为什么学习这个方法呢？因为他很好解决了多权重计算的问题。在做孕周这道题的时候，随机森林可以给出失败风险，延迟风险这些预测值但是
两者的权重却是我主观臆断给定的，并不严谨。引入多层次分析法无论是在线性还是非线性规划中都很有用！！！但是目前的困难点是数学知识跟不上，没有学过线性代数。跟着知乎上面的博主
学习也是学到了一些基础。这两天一边抄代码一边和豆包学每行代码的意思。抄完之后明天会去找一个例子实践。

Day 24 2025.12.26
写完了，原来的特征值方法有点问题。np.linalg.eig 方法会返回两个值，第一个返回值是矩阵的特征值，第二个返回值是矩阵的特征向量。原来的程序一直输出虚数，是因为在归一化步骤中
直接把特征值除以特征向量之和，导致只输出一个值而且为虚数。

Day 25 2025.12.27
今天完成的是模拟退火算法的初步探究。什么是模拟退火算法呢？先设定一个随机起点X，以及初始温度T。算法开始后，x随机向左或右移动一小格，相应的温度也要下降一些。
若移动后的y2<初始y1那么y2对应的x2成为新的x，继续这一个过程，直到温度T小于某一个特定值（阈值）。但是需要注意的是，模拟退火算法允许更差的解，但是需要概率辅助计算：
accept_prob = math.exp(-E_minus / T)。这有利于我们跳出局部最优解法去寻找全局最优解法。

Day 26 2026.1.4
新年快乐！好久不见，不是我偷懒了一直在学只是期末月比较忙没啥时间上传代码。那么这两天是写了一个最大递增子数列的程序。第一个函数利用贪心算法，只能统计连续的递增子数列长度
第二个函数利用递归思想可以统计非连续间断的递增子数列长度。那么在写这两个函数的时候我在同一个地方出现了思路不清晰的情况：何时更新最大长度。拿第一个函数举例来说， 
在循环体外应该先初始化一个global长度，这也是最后需要输出的数值。在第一个i循环之内，需要初始化一个从i开始统计的最大长度（索引从0，1，2，3开始都要重新初始化），
即max_length=1，那么在第二层循环跑完之后（统计完这一个分支的数值之后）就需要重新统计了。第二个函数的递归我还需要一些时间整理思路。

Day 27 2026.1.15
期末周结束!来写代码了，今天完成的是一个带有备忘录的递归，在函数中设置memo=None，若备忘录不为空则可以新增记录，例如4：5，5：8在每一次调用之后持续更新备忘录。这是一种自上而下的动态规划，
一种用空间换时间的算法，有效解决了重复子序列的问题。

Day 27 2026.1.15 
时间复杂度-(1)基本操作:O(1)  (2)顺序结构:加法计算  (3)循环结构:乘法计算
(4):分支结构：取最大值

Day 28 2026.1.16
今天是准备数模选拔。看见2024美赛C题我真的人都傻了，为什么有这么奇怪的数据我还不懂网球规则，但还是硬着头皮去看优秀论文。优秀论文中使用了GBDT的梯度提升树算法，就需要寻找特征列和目标列
特征列很好找但是目标列在题目给出的数据集中无法很好体现。那么目标列需要判断输赢。我先用if else+pandas新增了一个is_increae列，如下：
#新设定一个目标列
if  df['p1_score'].shift(-1) > df['p1_score'] or df['p1_score'] == 'AD':#若player1获胜
    df['is_increae'] = 1
else:
    df['is_increae'] = 0
那么这个错在哪里呢？就是不应当把逐行判断的思维带入pandas代码之中，df["列名"]是一个序列series,得到一整列布尔值，但是if语句是单值判断，会报ValueError
那么可以使用向量化操作：用 cond1 和 cond2 生成两个布尔 Series，再用 |（或）合并，最后转成整数 1/0。

Day 29 2026.1.17-1.18
学习GBDT梯度提升树，其实写法和随机森林并没有什么很大的区别，都可以利用网格搜索寻找最优解。需要注意的是，新版python将损失函数更新为 loss="log_loss"。梯度提升树其实就是一种
机器学习的迭代算法，通过构造一组弱学习器把各组预测结果累加作为预测输出。学习率（Learning Rate）是GBDT的重要超参数，通常取值在0到1之间。较小的学习率可以提高模型的鲁棒性，但需要更多的迭代次数。
为了防止过拟合，还可以通过限制树的深度、最小样本分割数等方式进行正则化。在这次编写代码过程中我灵活使用了不同函数和调用使其更清晰更易于维护，并且在函数文件中使用，if __name__="main"方法
检验函数重要性

Day 30 2026 1.20
在验证网球中势头的存在时，小组使用了游程检验+置信区间的z检验，但置信区间的前提是方差恒定，这一步是我们并没有尝试优化的。而且在置信区间之前我认为要先去判断样本是否属于正态分布，再去下定论。
其实挺简单的，只要把公式表达出来就行

Day 31 2026 1.22-1.23
肘部法则+k-means聚类。固定随机种子为42，其算法本质其实是分类的思想寻找局部最优，那我之前学的模拟退火是全局最优能不能结合一下呢！所以可以先用肘部法则确定聚类个数k,再利用模拟退火找到k个
更为合理的质心，相辅相成！但是在实际做的时候发现自己还是有点理想化了。在Day 25接触到的只是最简单最基础的模拟退火算法，只是一维的，在实际数据运用中至少是二维数据。根据SA的逻辑
先在数据中随即找k个质心，一开始只是用data[i].copy()找了前k个样本，但其实这样是很不严谨而且在后续容易让算法陷入局部最优。那么选择切片
indices = np.random.choice(len(data), size=k, replace=False)      #replace = false 表示不重复选择
centers = data[indices].copy()  这样就能保证质心的选择是随机的。之后在cal_ernergy函数里面计算所有点的能量大小。numpy库里面的linalg就是专门计算欧氏距离的！很方便，在k-means逻辑中最好用sse
即距离的平方和

Day 32 2026 1.30
休整一周开始猛攻数模。今年数模比赛的题目是一个选秀节目背景要求给出模型估计选手表现和被淘汰概率。在拿到题目之后我先把csv文件中所有的N/A值转为空值，还是依靠replace函数。把result列设为目标列
target_col = "result",之后我手动改了result里面400个数据……很麻烦。我知道其实可以用字符串实现(但是我目前掌握的不好，if cell_str[0].isdigit():#如果数字打头
return cell_str[0]    else:    return cell_str[0].capitalize())用一个函数分分钟解决。之后我把result列显示为1/2/3即前三名的小组单独拿出来构成一个新的excel。与之类似的把职业分为6类，用
数字1-6表示不同的职业。

Day 32 2026 1.30
在做问题一的时候我本来尝试去用随机森林做一个反向的尝试。对评委评分进行归一化处理，然后把最终综合得分即placement量化为score = placement.max-placement。但是粉丝票数其实是一个黑箱模型
很难去把他作为目标列。而且在随机森林中很难表现每一周的淘汰结果，而这毫无疑问是本题的难点。学姐给的思路是利用蒙特卡洛模拟，在分析每一季每一周的得分情况和淘汰情况下，在AI的帮助下
生成了100个符合淘汰情况的粉丝投票数量（百分数），最后3000个数据取中位数回代到原数据中看看准确率为多少

Day 33 2026 1.31
美赛第二天，精疲力竭兄弟们，队友给的模型其实我不太会写然后他们写的模型AI味特别重，感觉没有真正花时间理解。好回到专题里面，补充昨天的归一化处理，我使用的是最大最小归一化处理，即在组内
利用得分=（原始得分 - 组内最小值）/（组内最大值-组内最小值）。然后今天是写的随机森林，（补充其实可以用函数毕竟是两个差不多的训练模型）特征列目标列都是一致的。重点是注意二维数组和一维数组的
区别。因为特征列比较多，为了防止欠拟合采用网格搜索把树的深度稍微定深一些。

Day 34 2026 2.11
前两周去香港玩了，没更新。今天开始学习flask，希望能在之后做出一个公用网页可以在用户拖入excel表格之后自动处理数据并画图。遇到的第一个问题就是函数里面用return！才会输出到网页中，print仅仅是打印到终端。
第二个就是尝试了在关闭网页的同时结束程序运行，就需要利用global定义全局变量。复习一下global：只有在函数内部修改值（默认非只读）并且变量在函数外部定义

Day 35 2026.2.11
回顾一下面向对象，以及一些之前没有接触的处理元数据的代码。首先就是最最关键的init方法，在class一类中，self.name = name的作用就是给实例绑定专属属性，例如在处理列表时就要用init方法，传入同一个
文件路径，这个文件路径是特殊的所以就可以加入init函数，在其中就可以写下self.path = path。当然如果类中的都是通用的函数，例如通用工具类，里面加减乘除函数就可以没有init函数。

Day 36 2026.2.12
主要更新了一下pandas的那个文件，进阶学习了处理缺失值和归一化标准化。mean()平均值 median()中位数 mode()众数处理缺失值，需要注意的是，在使用inplace方法时，inplace = True修改的是内存中的dataframe，
实际的excel需要to_excel才能被修改。然后从sklearn库里面导入StandardScaler ,MinMaxScaler这两个工具就可以，第一个标准化第二个归一化。

Day 37 2026.2.13
把数据处理那个文件改成了面向对象的clean_data类，下设多个函数。在init中定义了self.df = pd.read_excel(r"")，那为什么要使用self的方法很重要呢？因为df只是 __init__ 方法内部的临时变量，__init__ 执行完，
这个 df 就被销毁了其他函数就无法访问了
